def radix_sort(to_be_sorted):
    maximun_value = max(to_be_sorted)
    max_exponent = len(str(maximun_value))

# Create a copy of to_be_sorted here:
    being_sorted = to_be_sorted[:]
    digits = [[] for digit in range(10)]
    return digits

    
    